export enum ReportDownloadStatus {
    Pending = 'PENDING',
    Running = 'RUNNING',
    Ready = 'READY',
    Failed = 'FAILED',
}

export enum ReportType {
    TransactionDetail = 'TransactionDetail',
}
